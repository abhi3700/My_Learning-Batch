@echo off
rem -------------------------------------------------------------------
rem This shows the hidden file, folders
rem NOTE: check the file if exist, then only run
rem -------------------------------------------------------------------
attrib -r -h -s Book1.xlsx